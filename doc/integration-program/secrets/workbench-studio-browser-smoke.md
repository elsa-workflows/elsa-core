# Workbench and Studio Secrets browser smoke

This is preparation evidence for #8326, not completion of the browser workflow or the history-preserving import. The [sanitized receipt](workbench-studio-browser-smoke-2026-09-25.json) records the exact source pins, mapped rehearsal commit, build hashes, private fixture policy, observations, and cleanup. The repeatable fixture guard and launch/cleanup procedure are in [Workbench Secrets runtime preflight](../../../scripts/integration-program/workbench-secrets-runtime-preflight.md).

The inspected sources were Core `a13ac7a412e037280d7a568fd9dd87b06ff8b724`, Extensions `ba8b71d91c15ffe5be4b2c539cf9f712e74af775`, and Studio `20ceaeeed7e671f0c9662003e82063026f2216de`, mapped in disposable rehearsal commit `372ed7ed3973c90fa581bf6003040900137bc093`. An unmodified current-main preflight helper (Git blob `270ed62513db488f60e17cb92f53b0db47e501ce`) accepted a private copy after scratch-only test modifications were removed; the prepared mapped workspace was untouched.

The mapped Workbench built and ran on `net10.0` with a fresh private SQLite database and two synthetic tenant configurations. Its health endpoint returned `200 Healthy`, and the identity login route exposed POST only. The mapped Studio server host built with 0 errors and 126 warnings. ClientLib's generated-file check passed, 253 Designer tests passed, and Designer and DomInterop bundles were emitted. The Studio sign-in page rendered with its Blazor WebSocket connected; direct loopback asset checks returned HTTP 200. These are startup and asset results, not authenticated UI results.

The browser sign-in **did not advance**. Submitting the synthetic tenant-A administrator left the page at `/login` with no snackbar or navigation. A temporary loopback forwarder recorded no request to the Workbench backend during the submit. A direct POST to the same backend endpoint using that synthetic account returned HTTP 200 with `isAuthenticated=true`; token presence was checked as booleans, and token values were not retained. The direct request proves backend authentication only. The available browser interface exposed console and asset observations but no Network panel, so the exact UI failure is unresolved. The Studio host defaults to `ElsaIdentity`, whose sign-in component returns silently when form validation fails before a backend request. The receipt did not record the selected runtime authentication provider or form-validation state, so that is a source-supported hypothesis, not a diagnosed cause.

The fixture used only private loopback ports, fresh data and lock directories, synthetic users and generated keys, an empty webhook sink list, and no provider credentials. All hosts and the temporary forwarder were stopped before the unmodified helper removed the fixture. No package was published and no production configuration changed. The receipt contains no passwords, JWTs, signing keys, encryption keys, or customer data.

Next, confirm the selected `Authentication:Provider` and trace the sign-in handler entry, form-validation result, and validator call with diagnostics that record no credential or token values. Then rerun a real browser session for allowed and denied users, both tenants, Secrets list/detail/create/update/rotate/revoke/delete/test, workflow picker behavior, and plaintext absence in responses and logs. Reapply the smoke after the canonical history-preserving import. Until then #8326 and its parent #8275 remain open; a successful API login or static asset build does not satisfy their Studio acceptance.
