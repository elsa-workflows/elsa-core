namespace Elsa.Studio.Enums;

/// <summary>
/// Provides field extension render positions.
/// </summary>
public enum FieldExtensionPosition
{
    /// <summary>
    /// Positions the extension at the top of the field.
    /// </summary>
    Top = 0,

    /// <summary>
    /// Positions the extension at the bottom of the field.
    /// </summary>
    Bottom = 1
}