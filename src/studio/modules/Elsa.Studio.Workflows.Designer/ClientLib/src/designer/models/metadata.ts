export interface ActivityDisplayTextMetadata {
    displayText?: string;
}

export interface ActivityDesignerMetadata {
    designer: {
        position: {
            x: number;
            y: number;
        };
    }
}