use expressions js;

workflow "HelloWorldHttpDsl2" {
    var message = "Hello World from DSL via Expressions!";
    listen HttpEndpoint("/hello-world-dsl-2");
    //WriteHttpResponse(js => `Message: ${message}`);
    WriteHttpResponse(message);
}